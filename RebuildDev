pipeline {
    agent any
    stages {
        stage('Delete dev branch') {
            steps {
                echo 'Deleting dev branch...'
                bat 'git status'
            }
        }
        stage('Rebuild dev branch') {
            steps {
                echo 'Rebuilding dev branch...'
            }
        }
    }
}